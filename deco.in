#!%GUILE% --no-auto-compile
-*- scheme -*-
!#

(set! %load-path (cons "%DMDDIR%" %load-path))
(set! %load-compiled-path (cons "%DMDDIR%" %load-compiled-path))
(apply (@ (deco) main) (cdr (command-line)))
